(deflisten window 
  :initial "..." 
  "bash ./scripts/hyprland.sh -t"
)

(defwidget window []
  (eventbox
    :class "window_w-event"
    (box
      :class "window"
      (label 
        :text "${window}"
        :limit-width 40
      )
    )
  )
)
